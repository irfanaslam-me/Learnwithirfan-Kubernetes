# 03. Core Concepts & First Commands

> **Series:** Overview → Install → **Core Concepts** → Deployments & ReplicaSets → Services → …

This document covers the three most important Kubernetes objects you will use every day — **Pod**, **Deployment**, and **Service** — and the commands to work with them.

---

## The Three Core Objects

```mermaid
graph TD
    DEP["🚀 Deployment\nManages Pods\nKeeps N copies running\nHandles updates & restarts"]
    RS["📦 ReplicaSet\nCreated automatically by Deployment\nCounts and replaces Pods"]
    POD["🟢 Pod\nRuns your container\nSmallest unit in Kubernetes"]
    SVC["🌐 Service\nStable address for the Pods\nLoad balances traffic to them"]
    USER["👤 User / Browser"]

    DEP -->|"owns"| RS -->|"creates"| POD
    SVC -->|"routes traffic to"| POD
    USER -->|"connects via"| SVC

    style DEP fill:#2980B9,color:#fff
    style RS fill:#27AE60,color:#fff
    style POD fill:#1ABC9C,color:#fff
    style SVC fill:#8E44AD,color:#fff
```

---

## 1. Pod — The Running Application

A Pod is the smallest deployable unit in Kubernetes. It contains one or more containers.

```mermaid
graph TD
    subgraph "Pod — one container (most common)"
        C1["📦 nginx container\nruns your web app"]
    end

    subgraph "Pod — two containers (advanced)"
        C2["📦 App container\nwrites logs"]
        C3["📦 Log sidecar container\nreads and ships those logs"]
        C2 <-->|"share same IP\nand storage"| C3
    end
```

Create a Pod directly (quick test):

```bash
kubectl run nginx --image=nginx
```

Check it:

```bash
kubectl get pods
```

Example output:

```
NAME                      READY   STATUS    RESTARTS   AGE
nginx                     1/1     Running   0          10s
```

> **Problem with bare Pods:** If the Pod crashes, Kubernetes does **not** restart it. That is why you use a Deployment.

---

## 2. Deployment — The Pod Manager

A Deployment manages Pods and makes sure the desired number are always running.

```mermaid
graph TD
    DEP["🚀 Deployment\nI want 3 nginx Pods running at all times"]
    P1["🟢 Pod 1 ✅"]
    P2["🟢 Pod 2 ✅"]
    P3["🟢 Pod 3 ✅"]

    DEP --> P1 & P2 & P3
```

If one Pod crashes, the Deployment creates a replacement automatically:

```mermaid
graph LR
    P1["🟢 Pod 1"] 
    P2["🟢 Pod 2"]
    P3["💥 Pod 3 crashes"]
    NEW["🟢 New Pod 3 created automatically ✅"]

    P3 -->|"gone"| NEW

    style P3 fill:#E74C3C,color:#fff
    style NEW fill:#2ECC71,color:#fff
```

Create a Deployment:

```bash
kubectl create deployment hello-k8s --image=nginx
```

Scale it up to 3 Pods:

```bash
kubectl scale deployment hello-k8s --replicas=3
```

```mermaid
graph LR
    BEFORE["Before\n1 Pod running"]
    CMD["kubectl scale --replicas=3"]
    AFTER["After\n3 Pods running"]

    BEFORE --> CMD --> AFTER
    style AFTER fill:#27AE60,color:#fff
```

**Benefits of Deployments:**
- Self-healing — replaces crashed Pods automatically
- Scaling — add or remove Pods with one command
- Rolling updates — update the image without downtime
- Rollbacks — go back to a previous version if something breaks

---

## 3. Service — Network Access to Pods

Pods get a random IP address that changes every time the Pod is replaced. A **Service** gives them a stable address that never changes.

```mermaid
graph LR
    subgraph "Without a Service — problem"
        P1A["Pod A\nIP: 10.0.0.1"]
        P1B["Pod A crashes\nnew IP: 10.0.0.4 ❌"]
    end
```

```mermaid
graph LR
    USER["👤 User"]
    SVC["🌐 Service\nhello-k8s\nstable IP: always the same"]
    P1["🟢 Pod 1\n10.0.0.1"]
    P2["🟢 Pod 2\n10.0.0.2"]
    P3["🟢 Pod 3\n10.0.0.3"]

    USER -->|"request"| SVC
    SVC -->|"load balances to"| P1 & P2 & P3

    style SVC fill:#8E44AD,color:#fff
```

Traffic is automatically load balanced:
```
Request 1 → Pod 1
Request 2 → Pod 2
Request 3 → Pod 3
Request 4 → Pod 1  (cycles back)
```

Expose the Deployment as a Service:

```bash
kubectl expose deployment hello-k8s --type=NodePort --port=80
```

Open it in your browser via Minikube:

```bash
minikube service hello-k8s
```

---

## The Full Flow

```mermaid
graph TD
    YOU["👨‍💻 You"]
    DEP["🚀 Deployment\nhello-k8s"]
    RS["📦 ReplicaSet"]
    P1["🟢 Pod 1"]
    P2["🟢 Pod 2"]
    P3["🟢 Pod 3"]
    SVC["🌐 Service\nhello-k8s"]
    BROWSER["🌐 Browser / User"]

    YOU -->|"kubectl create deployment"| DEP
    DEP --> RS --> P1 & P2 & P3
    YOU -->|"kubectl expose deployment"| SVC
    SVC -->|"routes to"| P1 & P2 & P3
    BROWSER -->|"minikube service hello-k8s"| SVC

    style DEP fill:#2980B9,color:#fff
    style SVC fill:#8E44AD,color:#fff
```

Your commands create this chain:

```bash
kubectl create deployment hello-k8s --image=nginx
# Creates: Deployment → ReplicaSet → Pod

kubectl expose deployment hello-k8s --type=NodePort --port=80
# Creates: Service → points at the Pods
```

---

## Commands Reference

### Listing resources

```bash
kubectl get pods           # list all Pods
kubectl get deployments    # list all Deployments
kubectl get svc            # list all Services
kubectl get all            # show everything at once
```

```bash
kubectl get pods -w        # watch pods — updates live in the terminal
```

### Checking a Service URL

```bash
minikube service hello-k8s        # opens in browser directly
minikube service hello-k8s --url  # prints the URL instead of opening browser
```

### Scaling

```bash
kubectl scale deployment hello-k8s --replicas=5   # scale up to 5
kubectl scale deployment hello-k8s --replicas=2   # scale down to 2
```

---

## Deleting Resources

### Delete a Service

```bash
kubectl delete svc hello-k8s
# or
kubectl delete service hello-k8s
```

```mermaid
graph LR
    subgraph "Before"
        DEP1["🚀 Deployment"] --> POD1["🟢 Pods"]
        SVC1["🌐 Service"] --> POD1
        USER1["👤 User"] --> SVC1
    end

    subgraph "After deleting Service"
        DEP2["🚀 Deployment"] --> POD2["🟢 Pods\nstill running"]
        NO["❌ No external access\nService is gone"]
    end
```

Deleting a Service only removes network access. The Deployment and Pods **keep running**.

---

### Delete a Deployment

```bash
kubectl delete deployment hello-k8s
```

```mermaid
graph TD
    DEL["kubectl delete deployment hello-k8s"]

    DEP["🚀 Deployment\n❌ deleted"]
    RS["📦 ReplicaSet\n❌ deleted"]
    P1["🟢 Pod 1\n❌ deleted"]
    P2["🟢 Pod 2\n❌ deleted"]
    P3["🟢 Pod 3\n❌ deleted"]

    DEL --> DEP --> RS --> P1 & P2 & P3

    style DEP fill:#E74C3C,color:#fff
    style RS fill:#E74C3C,color:#fff
    style P1 fill:#E74C3C,color:#fff
    style P2 fill:#E74C3C,color:#fff
    style P3 fill:#E74C3C,color:#fff
```

Deleting a Deployment removes **everything it owns** — the ReplicaSet and all Pods.

---

## Pod vs Container — Point of Confusion

Many people assume **Pod = Container**, but they are not the same thing.

```mermaid
graph TD
    subgraph "Docker world"
        DH["Docker Host"] --> DC["Container\nnginx"]
    end

    subgraph "Kubernetes world"
        KN["Node"] --> KP["Pod"] --> KC["Container\nnginx"]
    end
```

> Kubernetes adds an extra layer. The container lives **inside** the Pod.

In 90% of cases a Pod has just one container — so people casually say *"my Pod is running"* even though the container is what is actually doing the work.

### Why did Kubernetes create Pods?

Because sometimes two containers need to work very closely together:

```mermaid
graph LR
    subgraph "One Pod — two containers"
        APP["📦 Web App\n(nginx)\nwrites logs to /var/log"]
        LOG["📦 Log collector\n(Fluentd)\nreads logs from /var/log"]
        VOL["📂 Shared storage\n/var/log"]
        APP --> VOL --> LOG
        APP <-->|"same IP\nsame localhost"| LOG
    end
```

Both containers share the same IP address, same storage, and start/stop together. Kubernetes treats them as one unit.

---

## Real-World Analogy

| Kubernetes | Restaurant analogy |
|---|---|
| Pod | A chef — does the actual cooking |
| Deployment | The restaurant manager — ensures 3 chefs are always on shift |
| Service | The front desk — takes orders and sends them to an available chef |
| ReplicaSet | The shift tracker — counts chefs and calls in a replacement if one leaves |

---

## Quick Reference

```mermaid
mindmap
  root((kubectl))
    Get
      get pods
      get deployments
      get svc
      get all
      get pods -w
    Create
      run name --image
      create deployment name --image
      expose deployment --type --port
    Scale
      scale deployment --replicas
    Minikube
      service name
      service name --url
    Delete
      delete svc name
      delete deployment name
```

---

→ Next: [04. Deployments_Pods_ReplicaSets.md](04.%20Deployments_Pods_ReplicaSets.md)
